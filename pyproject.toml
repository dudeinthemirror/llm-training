[tool.poetry]
name = "llm-training"
version = "0.1.0"
description = ""
authors = ["dudeinthemirror"]
license = "Apache 2.0"
readme = "README.md"
package-mode = false

[tool.poetry.dependencies]
python = "^3.12.2"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
